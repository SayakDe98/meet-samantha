import React from "react";

const FeaturesSubTitle = () => {
  return (
    <span style={{ fontSize: 30, fontWeight: "bold", color: "red" }}>
      Built with Thousands of in-built tasks for you to choose from
    </span>
  );
};

export default FeaturesSubTitle;
